# Todo

- [x] 既存の画像読み込み・再生処理と関連ドキュメントを確認する
- [x] 縦・横の分割数を指定する UI を追加する
- [x] 画像を行優先で各フレームに分割して登録する
- [x] 1×1 の既存動作と複数行・複数列の分割をテストする
- [x] README にスプライトシートの使い方を追記する

## Review

- 「読み込み時の分割」に縦（行）・横（列）の指定欄を追加した。初期値は 1×1 のため既存動作を維持する。
- 左上から右方向、続いて次の行へ進む順序で分割し、一覧には `[行,列]` を表示する。
- 画像サイズが分割数で割り切れない場合も、端の画素を欠落させず全体をフレームへ割り当てる。
- `python -m unittest -v` の5テスト、`py_compile`、`git diff --check` がすべて成功した。
- オフスクリーン描画した画面を確認し、追加した指定欄が既存レイアウト内に収まることを確認した。

## スプライトシート出力

- [x] 既存の分割・再生実装とテスト構成を確認する
- [x] 出力する縦（行）・横（列）の指定 UI を追加する
- [x] 現在のフレームを行優先で1枚の画像に合成して保存する
- [x] 枠数不足、余り枠、異なるフレームサイズを検証する
- [x] README とレビュー記録を更新する

### Review

- 読み込み済みフレームを、指定した縦（行）×横（列）へ行優先で合成して保存できるようにした。
- 出力枠が余る場合は透明にし、フレームサイズが異なる場合は最大サイズのセル内で中央揃えにした。
- 枠数不足、空フレーム、大きすぎる出力画像、保存失敗をエラーとして扱う。
- 初回読み込み時はフレーム数と読み込みグリッドから、出力グリッドの初期候補を自動設定する。
- 合成順序・透明な余り枠・異なるサイズの中央配置・実ファイル保存を含む9テストが成功した。
- オフスクリーン描画で、出力欄が既存の 854×520 の画面内に収まることを確認した。

## プレビュー背景切替と個別PNG出力

- [x] 既存のプレビュー描画・保存処理と関連テストを確認する
- [x] 透明部分のプレビュー背景を白／黒から選べるようにする
- [x] フレームを `005_名前.png` から5刻みの連番で個別保存する
- [x] 入力検証、保存結果、画面レイアウトを確認する
- [x] README とレビュー記録を更新する

### Review

- プレビュー下部に「透明部分の背景」を追加し、黒（初期値）と白を即時に切り替えられるようにした。元画像と保存画像の透明度には影響しない。
- 個別PNG出力では保存ダイアログで指定したベース名を使い、一覧順に `005_名前.png`、`010_名前.png`…として保存する。
- `.png` の二重付与とWindowsで使用できないファイル名を防ぎ、同名ファイルがある場合は一括で上書きを確認する。
- 個別PNGの連番・並び順・透明度を含む全14テスト、`py_compile`、`git diff --check` が成功した。
- オフスクリーン描画で黒／白の両方を確認し、追加した操作欄が既存の 854×520 の画面内に収まることを確認した。
